Flask==3.0.2
Flask-Login==0.6.3
Flask-Migrate==4.0.7
Flask-SQLAlchemy>=3.1.1
SQLAlchemy>=2.0.39 # Use the specific version from upstream
Flask-WTF==1.2.1
Flask-APScheduler==1.12.3
email-validator==2.2.0
bcrypt==4.1.3 # Keep base bcrypt
python-dotenv==1.0.1
pymysql==1.1.2 # Use the specific version from upstream
requests==2.28.1 # Use the specific version from upstream
english-words==1.1.0 # Use the specific version from upstream
flask-bcrypt==1.0.1 # Keep Flask-Bcrypt
# --- Added from upstream ---
Pillow>=10.4.0 # Use Pillow (updated PIL fork), specify minimum version
qrcode>=7.4.2 # Specify minimum version
pyotp==2.8.0 # Use the specific version from upstream
# Add pyotp[qr] if you need QR code generation directly via pyotp, though qrcode lib does it too
# pyotp[qr]==2.8.0